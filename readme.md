# E2E tests

## inside the hood

* jest
* puppeeter
* cucumber (gherkin-jest)
* jest-puppeteer

dev:

* prettier
* eslint

## How to start

* `npm start` - run with headless chrome
* `npm run dev` - run with a visible chrome
